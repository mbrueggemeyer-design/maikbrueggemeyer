---
import "../styles/global.css";
import Sidebar from "../components/Sidebar.astro";

const personSchema = {
  "@context": "https://schema.org",
  "@type": "Person",
  "@id": "https://maikbrueggemeyer.de/#person",
  name: "Maik Brüggemeyer",
  url: "https://maikbrueggemeyer.de/",
  jobTitle: ["Autor", "Kulturjournalist", "Creative Director"],
  worksFor: {
    "@type": "Organization",
    name: "Rolling Stone Deutschland",
    url: "https://www.rollingstone.de/",
  },
  knowsAbout: [
    "Popmusik",
    "Literatur",
    "Film",
    "Popkultur",
    "Musikjournalismus",
  ],
};

interface Props {
  title?: string;
  description?: string;
  image?: string;
  structuredData?: Record<string, unknown>;
}

const {
  title = "Maik Brüggemeyer",
  description = "Geschichten zwischen Pop, Literatur, Film und Gesellschaft.",
  image = "/images/og-maik-brueggemeyer.png",
  structuredData,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const socialImageURL = new URL(image, Astro.site);

---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />

    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>{title}</title>
    <meta name="description" content={description} />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <link
      rel="alternate"
      type="application/rss+xml"
      title="Maik Brüggemeyer – Wohnzimmer"
      href="/rss.xml"
    />

<link rel="canonical" href={canonicalURL.toString()} />

<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:type" content="website" />
<meta property="og:url" content={canonicalURL.toString()} />
<meta property="og:image" content={socialImageURL.toString()} />
<meta property="og:site_name" content="Maik Brüggemeyer" />
<meta property="og:locale" content="de_DE" />

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={socialImageURL.toString()} />

    <script
      type="application/ld+json"
      set:html={JSON.stringify(personSchema)}
    ></script>

    {
      structuredData && (
        <script
          type="application/ld+json"
          set:html={JSON.stringify(structuredData)}
        ></script>
      )
    }
  </head>

  <body>
    <div class="site-shell">
      <Sidebar />

      <main class="site-main">
        <slot />
      </main>
    </div>
  </body>
</html>

<style>
  .site-shell {
    display: grid;
    grid-template-columns: var(--sidebar-width) minmax(0, 1fr);
    min-height: 100vh;
  }

  .site-main {
    min-width: 0;
  }

  @media (max-width: 800px) {
    .site-shell {
      display: block;
    }
  }
</style>